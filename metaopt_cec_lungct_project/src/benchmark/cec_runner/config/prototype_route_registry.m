function reg = prototype_route_registry()
% prototype_route_registry
% Canonical registry for prototype route-compare smoke.
% Why: keep route identity/status/description in one place.

    rows = {
        'ROUTE_A', 'ROUTE_A_DIFFERENTIAL_GENERATOR_BBO', 'BBO_route_a_differential_generator_bbo', true, 'implemented', ...
        'Route A: BBO + differential main generator (dual-main generation active: BBO candidate + DE candidate competition).';
        'ROUTE_B', 'ROUTE_B_DUAL_POPULATION_BBO', 'BBO_route_b_dual_population_bbo', false, 'placeholder', ...
        'Route B archived: fallback to V3 baseline kernel (implementation snapshot moved to archive).';
        'ROUTE_C', 'ROUTE_C_SUCCESS_HISTORY_POOL_BBO', 'BBO_route_c_success_history_pool_bbo', false, 'placeholder', ...
        'Route C archived: fallback to V3 baseline kernel (implementation snapshot moved to archive).'
    };

    reg = table( ...
        string(rows(:, 1)), ...
        string(rows(:, 2)), ...
        string(rows(:, 3)), ...
        logical(cell2mat(rows(:, 4))), ...
        string(rows(:, 5)), ...
        string(rows(:, 6)), ...
        'VariableNames', { ...
            'route_id', ...
            'algorithm_token', ...
            'entry_func', ...
            'is_implemented', ...
            'status', ...
            'route_note'});
end
