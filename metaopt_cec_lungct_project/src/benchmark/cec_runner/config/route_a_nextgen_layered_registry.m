function reg = route_a_nextgen_layered_registry()
% route_a_nextgen_layered_registry
% Mainline registry for next-generation Route A study.

    rows = {
        'A_FROZEN_BASELINE', 'A_MAINLINE_FIXED_BASELINE', 'BBO_route_a_budget_adaptive_baseline_bbo', 'budget_adaptive_only', ...
        '主线A：固定底座（Budget_adaptive only），作为frozen baseline。';
        'B_SUCCESS_HISTORY', 'B_MAINLINE_SUCCESS_HISTORY', 'BBO_route_a_budget_adaptive_success_history_bbo', 'budget_adaptive_success_history', ...
        '主线B：Budget_adaptive + success-history步长自适应。';
        'C_CONTROLLED_DISPERSAL', 'C_MAINLINE_CONTROLLED_DISPERSAL', 'BBO_route_a_budget_adaptive_success_history_dispersal_bbo', 'budget_adaptive_success_history_state_triggered_dispersal', ...
        '主线C：Budget_adaptive + success-history + 状态触发受控扩散（非全群体无条件扩散）。'
    };

    reg = table( ...
        string(rows(:, 1)), ...
        string(rows(:, 2)), ...
        string(rows(:, 3)), ...
        string(rows(:, 4)), ...
        string(rows(:, 5)), ...
        'VariableNames', {'layer_id', 'algorithm_token', 'entry_func', 'strategy_tag', 'layer_note'});
end
