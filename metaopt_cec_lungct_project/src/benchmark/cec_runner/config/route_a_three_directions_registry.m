function reg = route_a_three_directions_registry()
% route_a_three_directions_registry
% Registry for three Route A exploration directions and one control.

    rows = {
        'A_CURRENT_CTRL', 'A_CURRENT', 'BBO_route_a_current_bbo', 'control_current', ...
        'Control: current Route A behavior.';
        'DIR1_LATE_PBEST', 'A_LATE_PBEST', 'BBO_route_a_late_pbest_bbo', 'late_pbest_elite', ...
        'Direction 1: late pbest only on elite under trigger + greedy acceptance.';
        'DIR2_SAFE_CONSERVATIVE', 'A_SAFE_CONSERVATIVE', 'BBO_route_a_safe_conservative_bbo', 'safe_conservative', ...
        'Direction 2: conservative safe line with small-step and strong constraints.';
        'DIR3_BUDGET_ADAPTIVE', 'A_BUDGET_ADAPTIVE', 'BBO_route_a_budget_adaptive_bbo', 'budget_aware', ...
        'Direction 3: budget-aware pbest scheduling by stage and budget mode.'
    };

    reg = table( ...
        string(rows(:, 1)), ...
        string(rows(:, 2)), ...
        string(rows(:, 3)), ...
        string(rows(:, 4)), ...
        string(rows(:, 5)), ...
        'VariableNames', { ...
            'version_id', ...
            'algorithm_token', ...
            'entry_func', ...
            'strategy_tag', ...
            'version_note'});
end
