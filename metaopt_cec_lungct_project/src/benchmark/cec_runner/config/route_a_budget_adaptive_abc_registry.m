function reg = route_a_budget_adaptive_abc_registry()
% route_a_budget_adaptive_abc_registry
% Registry for Route A budget-adaptive A/B/C focused smoke.

    rows = {
        'A_BASELINE', 'A_BUDGET_ADAPTIVE_BASELINE', 'BBO_route_a_budget_adaptive_bbo', 'budget_adaptive_baseline', ...
        'A: baseline budget-adaptive Route A (anchor).';
        'B_SHSA', 'B_BUDGET_ADAPTIVE_SHSA', 'BBO_route_a_budget_adaptive_shsa_bbo', 'success_history_step', ...
        'B: add success-history step adaptation in mid/late stage.';
        'C_ARCHIVE_ESCAPE', 'C_BUDGET_ADAPTIVE_ARCHIVE_ESCAPE', 'BBO_route_a_budget_adaptive_archive_escape_bbo', 'archive_escape', ...
        'C: add lightweight archive and stagnation-triggered controlled long jump.'
    };

    reg = table( ...
        string(rows(:, 1)), ...
        string(rows(:, 2)), ...
        string(rows(:, 3)), ...
        string(rows(:, 4)), ...
        string(rows(:, 5)), ...
        'VariableNames', { ...
            'version_id', ...
            'algorithm_token', ...
            'entry_func', ...
            'strategy_tag', ...
            'version_note'});
end
