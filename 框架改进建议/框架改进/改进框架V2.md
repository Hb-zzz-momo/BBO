([Past chat](https://chatgpt.com/c/69b25b8c-7c9c-83a5-9bfb-0469fc2c1205))([Past chat](https://chatgpt.com/c/69b3592a-4810-83a9-aba0-9d2c51e0be23))([Past chat](https://chatgpt.com/c/69b765f2-c27c-83a4-9bbf-ad04606a265c))

### 下面这段可以直接发给 Codex

你现在要直接修改我仓库中的 MATLAB（矩阵实验室）代码，目标是**解决 6 个工程级 bug**，但**绝对不要改变现有算法数值逻辑、CEC（Congress on Evolutionary Computation，进化计算大会）测试语义、随机种子语义、函数评估次数（FEs, Function Evaluations）控制逻辑和已有实验结果格式语义**。

## 总目标

在保持 benchmark（基准测试）原始执行逻辑不变的前提下，对 `src/benchmark/cec_runner` 做一次“结构收口式重构”，解决以下 6 个问题：

1. `core/run_all_compare.m` 过大，已经是神文件（God File）
2. 输出目录分裂，pipeline（流程脚本）报告和真正数值结果不在同一棵实验树下
3. 入口和路径初始化重复，新旧入口并存且 pipeline 仍绕 legacy（旧兼容层）
4. 配置层半旧半新，存在 registry（算法注册表）/ profile（阶段模板）/ stage cfg（阶段配置）不一致
5. `baselines/` 目录过重，参考性第三方代码与正式运行代码边界不清
6. MATLAB 同名函数冲突风险仍在，只是暂时被 addpath/rmpath 压住

---

## 修改原则（必须遵守）

### 不允许做的事

- 不要改任何算法核心更新公式
- 不要改 CEC2017 / CEC2022 的目标函数调用语义
- 不要改 `maxFEs`、`pop_size`、`dim`、`runs`、`rng_seed` 的含义
- 不要把现有 benchmark 重写成 class（类）系统
- 不要把项目改成复杂包结构（除非只是很轻量的 private helper）
- 不要引入不必要的新依赖
- 不要删掉 legacy 文件，除非只是让它们退居兼容层
- 不要改动 `src/improved/algorithms/BBO` 中算法本体逻辑
- 不要改动 CEC `.mexw64`（MATLAB 扩展二进制）接口使用方式
- 不要做“看起来更优雅但高风险”的大重写

### 必须做到的事

- 保持原有实验逻辑和输出内容尽量兼容
- 重构以“低风险、可回滚、可验证”为第一原则
- 优先做“拆分结构、统一入口、统一输出树、补一致性检查”
- 所有新函数、辅助文件、目录命名必须清晰直接
- 每改一类问题，都要给出最小验证步骤
- 最终输出一份变更摘要，说明改了哪些文件、为什么改、怎么验证

---

## 必须先阅读和检查的文件

先完整阅读这些文件，再开始改：

### 核心执行层
- `src/benchmark/cec_runner/core/run_all_compare.m`
- `src/benchmark/cec_runner/core/run_experiment.m`
- `src/benchmark/cec_runner/core/run_suite_batch.m`
- `src/benchmark/cec_runner/core/setup_benchmark_paths.m`
- `src/benchmark/cec_runner/core/normalize_config.m`
- `src/benchmark/cec_runner/core/resolve_mode.m`

### 配置层
- `src/benchmark/cec_runner/config/default_experiment_config.m`
- `src/benchmark/cec_runner/config/resolve_experiment_mode.m`
- `src/benchmark/cec_runner/config/algorithm_registry.m`
- `src/benchmark/cec_runner/config/stage_profiles.m`

### pipeline 公共层
- `src/benchmark/cec_runner/pipeline_common/build_smoke_cfg.m`
- `src/benchmark/cec_runner/pipeline_common/build_formal_cfg.m`
- `src/benchmark/cec_runner/pipeline_common/fill_common_stage_cfg.m`
- `src/benchmark/cec_runner/pipeline_common/run_phase_via_unified.m`
- `src/benchmark/cec_runner/pipeline_common/save_stage_report.m`
- `src/benchmark/cec_runner/pipeline_common/write_stage_scan.m`

### pipeline 层
- `src/benchmark/cec_runner/pipelines/run_bbo_research_pipeline.m`
- `src/benchmark/cec_runner/pipelines/run_v3_direction_reduced_ablation.m`
- `src/benchmark/cec_runner/pipelines/run_v3_dual_objective_ablation.m`

### 入口与兼容层
- `src/benchmark/cec_runner/entry/run_main_entry.m`
- `src/benchmark/cec_runner/legacy/run_experiment_unified.m`
- `src/benchmark/cec_runner/legacy/run_compare_sbo_bbo.m`

### 文档层
- `src/benchmark/cec_runner/README.md`
- `src/benchmark/cec_runner/docs/README_entrypoints.md`
- `src/benchmark/cec_runner/docs/README_benchmark_flow.md`
- `src/benchmark/cec_runner/docs/README_ablation_flow.md`
- `src/benchmark/cec_runner/docs/REFACTOR_MAPPING.md`

### baseline 目录结构（用于路径与冲突检查）
- `src/baselines/metaheuristics/BBO/...`
- `src/baselines/metaheuristics/SBO/...`
- `src/baselines/other_metaheuristics/mdm-opt-main/...`

---

## 问题 1：拆分 run_all_compare.m，但保持逻辑不变

### 现状
`src/benchmark/cec_runner/core/run_all_compare.m` 约 1954 行，包含大量 local functions（本地子函数），职责过多：

- 默认配置
- 配置校验
- suite（测试套件）处理
- 路径处理
- 算法 inventory（清单）
- 单算法运行
- FE 计数包装
- run result（单次运行结果）组装
- summary 汇总
- manifest（运行清单）
- 绘图与 figure（图像）保存
- logging（日志）

### 目标
把它拆成“薄入口 + 若干 helper（辅助函数）文件”，但**run_all_compare.m 仍然是唯一 benchmark kernel（基准测试内核）入口**。

### 推荐拆分方式（优先低风险）
优先使用：
- `core/private/` 私有辅助函数目录  
因为 private 函数只对 `core/` 下调用者可见，最适合低风险拆分。

### 目标结构（可微调，但不要偏离）
建议至少拆成这些文件：

- `src/benchmark/cec_runner/core/run_all_compare.m`
- `src/benchmark/cec_runner/core/private/rac_config_and_suites.m`
- `src/benchmark/cec_runner/core/private/rac_paths_and_suite_api.m`
- `src/benchmark/cec_runner/core/private/rac_algorithm_inventory.m`
- `src/benchmark/cec_runner/core/private/rac_execution.m`
- `src/benchmark/cec_runner/core/private/rac_results_io.m`
- `src/benchmark/cec_runner/core/private/rac_plotting.m`
- `src/benchmark/cec_runner/core/private/rac_logging.m`

### 拆分要求
将 `run_all_compare.m` 中原有本地函数按职责迁出，例如：

#### 配置与 suite 相关
- `fill_default_config`
- `validate_config`
- `normalize_suite_list`
- `derive_suite_cfg`
- `resolve_function_ids_for_suite`
- `validate_func_ids_for_suite`

#### 路径与 suite api 相关
- `resolve_common_paths`
- `build_suite_api`
- `resolve_suite_dir`
- `get_cec2017_function`
- `get_cec2022_function`
- `call_getter_in_suite_dir`
- `eval_objective_in_suite_dir`
- `ensure_suite_getter_on_path`

#### 算法 inventory 与路径切换
- `setup_algorithm_paths`
- `setup_single_algorithm_path`
- `teardown_single_algorithm_path`
- `teardown_algorithm_paths`
- `is_path_entry_active`
- `build_algorithm_inventory`
- `is_legacy_alias_name`
- `get_algorithm_catalog`
- `make_algorithm_spec`
- `save_algorithm_inventory`
- `log_algorithm_inventory`

#### 运行执行与 FE 控制
- `make_counted_objective`
- `run_one_algorithm`
- `estimate_iteration_budget`
- `fe_mode_on_budget_stop`
- `fe_mode_on_runtime_error`
- `build_run_result`
- `build_manifest_row`
- `derive_run_seed`

#### 结果保存与 summary
- `init_result_dirs`
- `save_single_run`
- `save_curve_file`
- `build_summary_table`
- `build_run_manifest_table`
- `save_summary`

#### 绘图相关
- 所有 plot / figure / trace / radar / boxplot / convergence 相关函数

### 拆分后的约束
- `run_all_compare.m` 保持主流程清晰，尽量缩到 250~400 行以内
- 不改变外部调用方式：`output = run_all_compare(cfg)` 仍成立
- 所有 helper 命名加统一前缀，例如 `rac_`
- 新文件中要保留原函数注释语义
- 拆分后必须确保 smoke/formal 跑法结果一致

---

## 问题 2：统一输出目录树，让 pipeline 报告与真实结果放在同一实验根目录下

### 现状问题
当前 pipeline 层自己写 `out_root`，而 `run_all_compare` 又按 `cfg.result_root + suite + experiment_name` 单独落盘，导致：

- pipeline report 在一处
- summary/raw_runs/curves/logs 在另一处
- 同一个实验的证据链分裂

### 目标
统一成“单实验根目录”。

### 目标输出结构
请实现下面这种统一树：

```text
results/
  benchmark/
    research_pipeline/
      <experiment_name>/
        pipeline_scan.md
        smoke_stage_report.md
        formal_stage_report.md
        protocol_snapshot.mat
        cec2017/
          summary.csv
          summary.mat
          raw_runs/
          curves/
          logs/
          plots/
        cec2022/
          ...
```

对于其他 pipeline，例如 `v3_direction_reduced_ablation`，也遵循同一模式：

```text
results/
  benchmark/
    v3_direction_reduced_ablation/
      <experiment_name>/
        smoke_stage_report.md
        formal_stage_report.md
        cec2017/
          ...
```

### 实现要求

引入统一的结果路径协议，不要再让 pipeline 自己和 core 各写一套目录逻辑。

建议新增统一字段（命名可稍调，但语义必须有）：

- `cfg.result_root`：例如 `'results'`
- `cfg.result_group`：例如 `'benchmark/research_pipeline'`
- `cfg.experiment_name`
- `cfg.result_layout`：可选，默认 `'experiment_then_suite'`

然后让核心目录初始化逻辑统一生成：

```matlab
<repo_root>/<result_root>/<result_group>/<experiment_name>/<suite_name>/
```

同时让 pipeline 层的：

- stage report
- stage scan
- markdown 汇总
- protocol snapshot
- 任何 pipeline 附加说明

全部写入：

```matlab
<repo_root>/<result_root>/<result_group>/<experiment_name>/
```

而不是再自己另外拼一棵树。

### 具体修改点

重点检查并修改：

- `core/run_all_compare.m` 中 `init_result_dirs`
- `config/resolve_experiment_mode.m`
- `pipeline_common/build_smoke_cfg.m`
- `pipeline_common/build_formal_cfg.m`
- `pipelines/run_bbo_research_pipeline.m`
- `pipelines/run_v3_direction_reduced_ablation.m`
- `pipelines/run_v3_dual_objective_ablation.m`

### 兼容要求

- 默认情况下，普通 `run_main_entry` 仍可输出到简洁路径
- 但 pipeline 模式下必须走统一实验树
- 尽量不破坏已有字段名；如果新增字段，要提供向后兼容默认值

------

## 问题 3：收口入口与路径初始化，pipeline 不再绕 legacy

### 现状问题

当前存在多层入口：

- 人工入口：`entry/run_main_entry.m`
- 核心入口：`core/run_experiment.m`
- legacy 兼容入口：`legacy/run_experiment_unified.m`
- pipeline 桥接：`pipeline_common/run_phase_via_unified.m`

而且 `run_phase_via_unified.m` 仍在调用 `run_experiment_unified`，也就是 pipeline 仍绕 legacy 壳。

### 目标

形成清晰单一入口规范：

#### 允许保留

- 人工主入口：`entry/run_main_entry.m`
- 系统执行入口：`core/run_experiment.m`
- 兼容入口：`legacy/run_experiment_unified.m`（保留，但不再是主路径）

#### 必须实现

- pipeline 默认直接调用 `core/run_experiment.m`
- `run_experiment_unified.m` 只做 legacy 兼容
- `setup_benchmark_paths.m` 成为唯一路径 bootstrap（启动初始化）
- 删除或替换重复的 addpath 逻辑
- 不允许 pipeline 继续通过 legacy 入口跑主链

### 具体修改方案

1. 新增：
   - `pipeline_common/run_phase_via_core.m`
2. 它的职责：
   - 接收 phase_cfg
   - 构造 `core/run_experiment.m` 需要的 cfg
   - 直接调用 `run_experiment(cfg)`
3. 将：
   - `run_bbo_research_pipeline.m`
   - `run_v3_direction_reduced_ablation.m`
   - `run_v3_dual_objective_ablation.m`
     中默认执行路径改为走 `run_phase_via_core`

### 对 legacy 的处理

- `run_experiment_unified.m` 保留
- 但仅作为兼容层，不再由 pipeline 默认依赖
- 可以在注释或 warning 中明确说明：legacy only, not default pipeline path

### 路径初始化要求

确保只有 `setup_benchmark_paths.m` 负责：

- `core`
- `config`
- `export`
- `compat`
- 必要的 runner 根路径

让以下文件不再各自重复 addpath 拼装：

- `entry/run_main_entry.m`
- `legacy/run_experiment_unified.m`
- `pipeline_common/run_phase_via_unified.m`
- 各 pipeline 内部本地 wrapper

如果必须保留极少量 addpath，也要统一风格，只能调用 `setup_benchmark_paths()`，不要散落写 `fileparts + addpath`。

------

## 问题 4：清理半旧半新配置，形成统一配置协议

### 现状问题

当前存在：

- `default_experiment_config.m`
- `resolve_experiment_mode.m`
- `fill_common_stage_cfg.m`
- `algorithm_registry.m`
- `stage_profiles.m`

但它们之间仍有轻微不一致，例如：

- `algorithm_registry('research_core')` 已不含某些旧项
- 但其他脚本仍扫描旧版本或半退休版本
- `stage_profiles('v3_dual_ablation')` 的 suites 只用 `cec2017`，却还挂着 `cec2022` 的 func_ids 结构

### 目标

明确一套统一配置协议：

#### 规则 1

`default_experiment_config.m` 是全局默认值唯一来源

#### 规则 2

`fill_common_stage_cfg.m` 只补阶段脚本特有字段，不再重复定义全局默认值语义

#### 规则 3

`algorithm_registry.m` 只维护“正式注册算法”
如果某算法是 retired（退役）/ deprecated（弃用）/ archived（历史版本），要明确标记，不要半活不活

#### 规则 4

`stage_profiles.m` 中的 suite 与 func_ids 必须一致
不能出现 suite 只跑 `cec2017`，profile 结构却挂 `cec2022` 的无效字段

### 具体要求

#### A. 统一默认值来源

检查并整理：

- `default_experiment_config.m`
- `fill_common_stage_cfg.m`
- `resolve_experiment_mode.m`

确保：

- `dim`
- `pop_size`
- `maxFEs`
- `rng_seed`
- `result_root`
- `plot`
- `save_curve`
- `save_mat`
- `save_csv`
- `export`
  这些默认行为语义一致

#### B. 清理 V2 / 历史版本的半退休状态

检查：

- `config/algorithm_registry.m`
- `pipelines/run_bbo_research_pipeline.m`
- 任何 scan 或 docs 中是否仍默认把 V2 当活跃主线

要求：

- 如果 V2 不是正式主线，就不要让默认 profile 继续把它当活跃项
- 如需保留，只作为历史说明或显式选择项，不要混进默认主流程

#### C. 修正 `stage_profiles.m`

对于：

- `v3_dual_ablation`
  如果实际只跑 `cec2017`，则不要再保留 `cec2022` 的 smoke/formal func_ids
  让 profile 内容和实际 suite 完全一致

#### D. 精简 `_common` 套壳

检查这些文件：

- `build_formal_cfg_common.m`
- `build_smoke_cfg_common.m`
- `fill_common_stage_cfg_common.m`
- `run_phase_via_unified_common.m`

如果它们只是 1 层转发壳：

- 可以保留兼容
- 但主流程不要再依赖它们
- 在文档中标记为 compatibility shim（兼容转接层）
- 如果安全，可以减少实际调用链

------

## 问题 5：给 baselines 做“运行时边界收口”，把参考性大目录移出主运行树

### 现状问题

当前：

- `src/baselines/other_metaheuristics/mdm-opt-main/` 很大
- 还有 zip 包残留
- 这些内容并未真正纳入当前 MATLAB benchmark 主流程
- 但它们混在 `src/` 内，容易污染工程边界

### 目标

把“正式运行依赖”和“仅供参考的外部资料”分开。

### 要求

请实施以下收口方案：

#### A. 建立清晰目录边界

建议新增 repo 级目录（命名可略调）：

```text
third_party/
  active_matlab/
  reference_only/
archives/
```

或更稳妥一点：

```text
external/
  runtime/
  reference/
archives/
```

核心是语义要清楚：

- runtime / active：当前 benchmark 真正要用的
- reference_only：只参考，不参与运行
- archives：压缩包备份

#### B. 迁移内容

优先处理：

- `src/baselines/other_metaheuristics/mdm-opt-main/`
- `src/baselines/other_metaheuristics/mdm-opt-main.zip`

如果它们不是当前 MATLAB benchmark 运行依赖，则移动到：

- `external/reference/` 或 `third_party/reference_only/`
- zip 移到 `archives/`

#### C. 保持运行路径最小化

对当前 benchmark 真正需要的 baseline，只保留明确运行路径：

- BBO
- SBO
- MGO
- PLO
  以及 catalog 中明确登记的算法

不要让无关参考目录留在主运行树里。

#### D. 更新路径引用

如果移动了目录，请同步修改：

- `run_all_compare.m` 中 algorithm catalog 路径
- 任何 docs / scan 脚本 / README 中相关路径说明

### 注意

- 这是“工程边界整理”，不是要删除资料
- 不要误删任何文件
- 如果不确定是否被运行依赖，就先搜索引用，再决定迁移

------

## 问题 6：显式加固 MATLAB 同名函数冲突防线

### 现状问题

当前存在大量同名函数：

- `initialization.m`
- `main.m`
- `Get_Functions_details.m`
- `func_plot.m`

虽然当前 runner 通过单算法 addpath / rmpath 在尽量隔离，但这仍是脆弱稳定。

### 目标

让路径切换和冲突检查从“隐式依赖经验”变成“显式校验机制”。

### 必须实现的加固措施

#### A. 明确禁止 genpath 扫整棵 baseline 树

全项目检查一遍：

- 不允许对 baseline 总目录做 `addpath(genpath(...))`
- 只允许按 algorithm_dir 精确 addpath

#### B. 在单算法路径启用时做冲突校验

新增一个运行时检查函数，例如：

- `core/private/rac_validate_algorithm_path_resolution.m`
  或
- `core/validate_runtime_path_conflicts.m`

用途：
在某个 algorithm_dir 被激活后，检查关键名字的解析是否唯一且指向当前算法目录。

重点检查这些关键函数名：

- `initialization`
- `main`
- `Get_Functions_details`
- `func_plot`

要求：

- 用 `which(name, '-all')` 或等效方式检查
- 如果发现同名函数解析到多个活动目录，给出 warning 或 error
- 至少写入运行日志

#### C. 将路径切换封装更明确

检查并强化：

- `setup_single_algorithm_path`
- `teardown_single_algorithm_path`
- `setup_algorithm_paths`
- `teardown_algorithm_paths`

要求：

- addpath 后立即校验路径状态
- teardown 后确认路径已撤销
- 如果目录未成功移除，也要写日志

#### D. 新增一个静态检查脚本

新增一个脚本，例如：

- `src/benchmark/cec_runner/tools/check_path_collisions.m`

功能：

- 扫描 `src/baselines` 及相关 active runtime baseline 目录
- 找出高风险重名函数
- 输出一个 markdown 或 txt 报告

这不是运行时必需，但便于自检。

------

## 额外要求：补一份一致性自检

请额外新增一个轻量级自检入口，例如：

- `src/benchmark/cec_runner/tools/selfcheck_runner_integrity.m`

至少检查这些内容：

1. 主入口是否唯一清晰
2. pipeline 是否默认走 `run_experiment` 而非 legacy
3. result 根目录协议是否统一
4. registry 中算法项是否都能在 catalog 中解析
5. `stage_profiles` 的 suites 与 func_ids 是否一致
6. 同名函数风险报告是否能输出
7. `run_all_compare.m` 是否已明显瘦身
8. smoke 跑法是否还能通

输出一份简洁报告即可。

------

## 修改后的目标状态

最终希望达到下面这个工程形态：

### 入口层

- 人工：`entry/run_main_entry.m`
- pipeline：直接走 `core/run_experiment.m`
- legacy：只保留兼容

### 核心层

- `run_all_compare.m` 只保留主编排逻辑
- 大量 helper 拆到 `core/private/`

### 配置层

- 默认值唯一来源清晰
- stage profile 与 suite 一致
- registry 没有半旧半新的混乱状态

### 结果层

- 一个实验只有一棵实验根目录
- pipeline report 和 suite 结果都在同一个实验目录下

### baseline 层

- runtime baseline 与 reference-only 代码边界清晰
- 不相关的大型参考目录移出主运行树

### 路径安全

- 不依赖经验式 addpath
- 有显式冲突检查与日志

------

## 验收标准（必须逐项验证）

### 验收 1：主入口仍可跑

执行一个最小 smoke：

```matlab
cfg = struct();
cfg.mode = 'smoke';
report = run_main_entry(cfg);
```

要求：

- 能正常进入 `run_experiment -> run_suite_batch -> run_all_compare`
- 不绕 legacy 作为主链
- 正常输出结果

### 验收 2：pipeline 默认不再走 legacy

以任一 pipeline 跑 smoke，确认：

- 主调用链是 `run_phase_via_core -> run_experiment`
- `run_experiment_unified` 不再是默认链路

### 验收 3：结果目录统一

任意一个 pipeline 运行后，必须能看到：

```text
<experiment_root>/
  smoke_stage_report.md
  formal_stage_report.md (如有)
  cec2017/
  cec2022/ (如有)
```

而不是散落到不同树下。

### 验收 4：run_all_compare 明显瘦身

- `run_all_compare.m` 显著缩短
- helper 被迁移到 `core/private/`
- 逻辑可读性变好，但外部行为不变

### 验收 5：配置一致性改善

- `stage_profiles('v3_dual_ablation')` 中 suite 与 func_ids 一致
- 默认配置来源清楚
- 默认主线不再混入不该默认活跃的旧版本

### 验收 6：路径冲突防线可用

- 新增 collision check（冲突检查）脚本
- 运行时日志中可看到关键同名函数校验结果
- 不存在对 baseline 根目录的粗暴 `genpath`

------

## 输出要求

修改完成后，不要只给我一句“已完成”。

请输出：

### 1. 变更摘要

- 改了哪些文件
- 新增了哪些文件
- 删除或降级了哪些旧调用链
- 哪些地方只是兼容保留

### 2. 结构说明

说明新的执行链、结果树、配置链分别是什么

### 3. 风险说明

指出本次改动中仍存在的残余风险

### 4. 验证说明

写出你实际建议我执行的 smoke 验证命令

### 5. 如果发现某一步不确定

不要擅自编造
请在代码里用清晰注释标记 `TODO(low-risk follow-up)`，并在总结里说明

------

## 最后提醒

这次任务不是“写新算法”，而是“把现有 benchmark 工程收口”。
评价标准不是花哨，而是：

- 逻辑不变
- 风险可控
- 入口清晰
- 输出统一
- 配置一致
- 路径安全
- 后续更方便继续做 BBO（Beaver Behavior Optimizer，河狸行为优化）改进实验和论文复现

现在开始执行。

```
### 给 Codex 的补充说明

如果想再加一句“强约束版开场白”，可以把下面这一段放在最前面：

```text
请不要先给方案讨论，直接在仓库里实施最小风险修改。优先保证 benchmark 主链还能跑，再做结构收口。所有改动必须围绕“保持原始算法逻辑不变”展开。
```