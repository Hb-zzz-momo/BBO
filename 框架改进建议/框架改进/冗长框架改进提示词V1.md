([Past chat](https://chatgpt.com/c/69b69155-a644-83a7-b333-c34a39350de5))([Past chat](https://chatgpt.com/c/69b25b8c-7c9c-83a5-9bfb-0469fc2c1205))([Past chat](https://chatgpt.com/c/69b3b07b-148c-83aa-82a8-a7c1233043a1))([Past chat](https://chatgpt.com/c/69b296b4-c6cc-83a4-bb60-f3525fcd383c))

### 结论先说

可以，下面这版就是 **给 Codex 直接执行的“大胆重构但逻辑冻结版”提示词**。
核心思想是：

- **大胆整理结构**
- **统一入口、配置、导出、路径初始化**
- **保留原始算法逻辑、原始 CEC（Congress on Evolutionary Computation，进化计算大会）测试逻辑、原始统计语义不变**
- **旧入口先保留为兼容壳（wrapper，兼容包装层）**，避免一下子改崩

这比“继续最小改动往上叠”更适合你当前阶段。

------

### 给 Codex 的主提示词（可直接复制）

```text
你现在是这个 MATLAB 科研代码仓库的“结构化重构工程师 + 科研实验框架维护者”。

## 一、任务目标

对当前仓库做一次“大胆但受控”的结构化重构，目标是：

1. 显著降低 benchmark（基准测试）执行链的层级复杂度；
2. 收口实验入口，减少 run_*.m 入口爆炸；
3. 将路径初始化、配置标准化、模式解析、协议快照导出、实验总结导出等职责拆开；
4. 把 smoke / formal / ablation 等实验模式尽量改为“配置驱动”，而不是“代码分叉驱动”；
5. 保持原始算法逻辑、原始 CEC 测试逻辑、原始统计逻辑、原始结果语义不变；
6. 保留旧入口函数作为兼容层，避免现有调用全部失效；
7. 最终让项目更适合后续继续做 BBO（Beaver Behavior Optimizer，河狸行为优化）/ HHO（Harris Hawks Optimization，哈里斯鹰优化）改进实验与论文复现。

## 二、最高优先级约束（绝对不能违反）

### 1. 不改变以下“原始逻辑”
- 不改变 baseline（基线算法）与 improved（改进算法）内部优化更新规则；
- 不改变 CEC 测试函数调用逻辑；
- 不改变 run_all_compare 当前的核心 benchmark 执行语义；
- 不改变已有 summary（统计汇总）、ranking（排名）、curve（收敛曲线）、raw_runs（原始运行）等结果的统计口径；
- 不改变已有实验结果目录的核心含义；
- 不擅自改动会导致论文结果不可复现的随机种子语义、FEs（Function Evaluations，函数评估次数）语义、runs（重复次数）语义、维度 dim、种群规模 pop_size 等协议字段含义。

### 2. 允许做的事
- 可以重组文件结构；
- 可以新增 core / export / wrappers / configs 等层次；
- 可以提取公共函数；
- 可以让旧入口函数变成薄包装层；
- 可以增加兼容注释、迁移说明、README；
- 可以新增 smoke 测试脚本、自检脚本；
- 可以把“模式差异”改造成配置差异；
- 可以抽离 protocol_snapshot（协议快照）导出逻辑；
- 可以统一 addpath / 路径初始化逻辑。

### 3. 禁止做的事
- 禁止“为了简洁”偷偷改算法行为；
- 禁止重写 CEC runner 的核心执行规则；
- 禁止删掉旧入口但不提供兼容替代；
- 禁止把结果导出字段改名到无法对齐旧结果；
- 禁止引入与当前仓库无关的大型框架；
- 禁止把临时重构做成难以验证的纯风格改动。

## 三、仓库理解重点

当前仓库已经出现典型的“最小改动策略后期叠层”问题：
- 多个 run_*.m 入口并存；
- 配置逻辑、模式逻辑、导出逻辑、tracking（跟踪记录）逻辑交织；
- 运行主链越来越高；
- 阶段性实验脚本和长期框架文件混住。

这次重构的目标不是“重写项目”，而是：
“保留功能和结果语义，压缩层级，统一职责，减少重复入口”。

## 四、希望重构后的结构方向

优先朝下面这个方向收敛，但要结合实际仓库内容微调，不要机械照搬：

metaopt_cec_lungct_project/
├─ experiments/
│  ├─ configs/
│  │  ├─ benchmark/
│  │  ├─ ablation/
│  │  ├─ ct_app/
│  │  └─ sensitivity/
│  └─ scripts/
│     ├─ run_benchmark.m
│     ├─ run_ablation.m
│     ├─ run_ct_app.m
│     └─ run_sensitivity.m
│
├─ src/
│  ├─ benchmark/
│  │  ├─ core/
│  │  │  ├─ run_experiment.m
│  │  │  ├─ normalize_config.m
│  │  │  ├─ resolve_mode.m
│  │  │  ├─ setup_paths.m
│  │  │  └─ run_suite_batch.m
│  │  ├─ export/
│  │  │  ├─ save_protocol_snapshot.m
│  │  │  ├─ export_aggregate.m
│  │  │  └─ export_summary_md.m
│  │  ├─ wrappers/
│  │  └─ metrics/
│  │
│  ├─ improved/
│  ├─ baselines/
│  └─ medimg/
│
├─ results/
├─ docs/
├─ tracking/
└─ archive/

注意：这是“目标方向”，不是死规定。核心是职责解耦，而不是目录表演。

## 五、必须完成的重构动作

### A. 收口实验入口
把 benchmark 执行主入口尽量收口成一个统一入口，例如：
- src/benchmark/cec_runner/core/run_experiment.m
或在现有命名风格下的等价位置。

这个统一入口只负责：
1. 读取/接收配置；
2. 标准化配置；
3. 解析实验模式；
4. 调用底层 benchmark runner；
5. 调用导出器；
6. 返回 report。

不要在这个主入口里写过多导出细节和阶段性实验特例。

### B. 提取路径初始化
新增统一路径初始化函数，例如：
- setup_benchmark_paths.m

职责：
- 找到仓库根目录；
- addpath 必要目录；
- 返回 paths 结构体；
- 避免多个入口重复写 mfilename / fileparts / addpath。

### C. 提取配置标准化
新增统一配置标准化函数，例如：
- normalize_config.m
或基于现有 default_experiment_config.m 做规范化重构。

职责：
- 补默认值；
- 校验字段；
- 给缺失 export 开关、模式字段、结果根目录等赋默认值；
- 保证后续流程只面对“完整 cfg”。

### D. 提取模式解析
新增统一模式解析函数，例如：
- resolve_mode.m
或在现有 resolve_experiment_mode.m 基础上重构。

职责：
- 把 smoke / formal / ablation / research_pipeline 等差异，尽量转成配置映射；
- 输出 run_cfg 与 mode_info；
- 不把模式分支散落在多个 run_*.m 中。

### E. 抽离协议快照导出
把 protocol_snapshot.mat / protocol_snapshot.csv 的写出逻辑抽到独立函数，例如：
- save_protocol_snapshot.m

要求：
- 保持原字段含义不变；
- MAT 与 CSV 双输出保留；
- 原有字段名尽量保持兼容。

### F. 抽离总结导出
把 experiment_summary.md、aggregate summary 等导出逻辑从主入口中抽离，形成 export 层函数。

### G. 旧入口保留为兼容壳
对当前已有的这些入口风格文件，不要直接粗暴删除：
- run_experiment_unified.m
- run_bbo_research_pipeline.m
- run_v3_direction_reduced_ablation.m
- run_v3_dual_objective_ablation.m
- 其他 run_*.m

做法：
1. 保留文件；
2. 在文件头部加注释说明“兼容入口/推荐迁移到新统一入口”；
3. 内部尽量改成调用新的统一入口和配置生成逻辑；
4. 保持原调用方式仍能跑通，至少语义兼容。

### H. 阶段脚本与长期框架分离
把“长期框架逻辑”放到 src/benchmark/... 的 core / export / wrappers；
把“本次实验怎么组织”的脚本放到 experiments/scripts；
不要让某次 v3/v4 实验脚本承担长期框架职责。

## 六、重构策略要求

### 第一原则：先兼容，再收敛
不要一步删除旧文件。
正确策略是：
- 新增核心层；
- 旧入口转调新核心；
- 跑通后再逐步减薄旧逻辑。

### 第二原则：先抽公共逻辑，再整理目录
优先抽这些公共逻辑：
- 路径初始化；
- 配置标准化；
- 模式解析；
- 协议快照保存；
- 导出 summary；
- 统一 report 组装。

### 第三原则：不为了“好看”而牺牲证据链
results / docs / tracking / archive 不是本次主要重构对象。
不要为了目录整齐破坏已有实验记录与研究证据链。

## 七、希望你先做的实际分析

在真正修改前，先完成以下分析并再实施：

1. 扫描所有 run_*.m 的调用关系；
2. 识别哪些文件是真正总入口，哪些只是阶段脚本，哪些只是包装层；
3. 识别配置逻辑分散点；
4. 识别导出逻辑分散点；
5. 给出一份“最小风险重构映射表”：
   - 旧文件 -> 新职责/新位置/是否保留兼容层；
6. 再开始修改代码。

## 八、实施顺序（严格按顺序做）

### Step 1
先建立新的 core / export 层函数骨架，不改算法逻辑。

### Step 2
把路径初始化、配置标准化、模式解析抽出来。

### Step 3
让 run_experiment_unified.m 先变成“调用新核心的兼容壳”。

### Step 4
把其他 run_*.m 入口逐步转成兼容壳或阶段脚本。

### Step 5
把 protocol_snapshot / summary markdown / aggregate export 抽离成独立函数。

### Step 6
增加一个 smoke 自检脚本，验证重构前后 benchmark 输出语义一致。

## 九、验证要求（非常重要）

你必须设计并执行以下验证思路，保证“结构变了，逻辑没变”：

### 1. API 兼容验证
验证旧入口函数仍可调用，且不会立即失效。

### 2. 配置一致性验证
同一组 cfg 输入，重构前后解析出的 run_cfg 核心字段一致：
- suites
- dim
- pop_size
- maxFEs
- runs
- rng_seed
- experiment_name
- result_root
- save_curve
- save_mat
- save_csv
- mode
- wrapper 相关字段

### 3. 输出语义一致性验证
至少对 smoke 配置做一次对比，确认以下内容结构一致：
- summary.csv / summary.mat
- ranking_summary.csv
- raw_runs
- curves
- protocol_snapshot
- report markdown（若开启）

### 4. 算法行为未变验证
不要通过改动算法实现来“适配新框架”。
算法文件若有改动，只允许：
- 路径适配；
- 注释；
- 极少量非行为性接口兼容处理。

## 十、输出要求

请按下面格式交付，不要只埋头改代码不说明：

### 第一部分：重构计划
- 当前问题摘要
- 拟采用的新结构
- 旧文件到新职责的映射表

### 第二部分：实际修改
- 修改了哪些文件
- 新增了哪些文件
- 哪些旧文件保留为兼容层
- 哪些函数被抽离

### 第三部分：风险说明
- 哪些地方最可能破坏兼容性
- 你如何避免改变原始逻辑

### 第四部分：验证结果
- 做了哪些自检
- 哪些验证已完成
- 哪些验证因环境限制未完成，但给出明确复核方法

### 第五部分：迁移建议
- 后续新增实验应如何写
- 新实验是新增配置，还是新增脚本，还是新增核心函数

## 十一、编码风格要求

- 保持 MATLAB 风格一致；
- 不要引入花哨抽象；
- 函数名清晰直接；
- 注释优先说明“为什么这样做”；
- 对每个新增核心函数，写清职责、输入、输出、与旧逻辑的对应关系；
- 尽量少改动 run_all_compare 的核心主体；
- 允许通过包装层和抽公共函数实现重构；
- 尽量让主入口函数控制在清晰短小的长度。

## 十二、成功标准

这次任务成功，不是看“删了多少代码”，而是看：
1. 主执行链是否更短；
2. 入口是否更统一；
3. 配置/模式/导出是否解耦；
4. 旧调用是否尽量兼容；
5. CEC 与原算法逻辑是否保持不变；
6. 后续做 v3/v4/BBO 改进实验时，是否不需要再继续往上叠壳。

现在开始：
先分析仓库并给出“重构计划 + 文件映射表”，再实施修改；修改后给出验证结果与迁移说明。
```

------

### 给 Codex 的补充要求（建议再单独发一条）

```text
补充要求：

1. 这次重构优先服务于科研实验复现，不是纯代码美化；
2. 任何可能影响旧实验结果可复现性的改动，都要极度保守；
3. 优先把旧入口变薄，而不是一次性删除；
4. 若发现某些 run_*.m 的职责难以一次归并，先保留兼容壳，并在注释中明确“后续可继续收敛”；
5. 对新增的核心函数，请写清“原来逻辑在哪，现在搬到哪”；
6. 对 experiments/scripts 与 experiments/configs，请给出推荐使用方式；
7. 若仓库中已有 config / export 目录，则优先复用并规范化，不要无意义重命名；
8. 若发现某些文件名已经被研究记录、README、tracking 引用，优先保留兼容名；
9. 对 README_research.md / PROJECT_STRUCTURE_GUIDE.md 这类文档，按新的执行链补充更新；
10. 最终交付时请明确告诉我：后续新增一次正式实验，应该新增“配置文件”还是“脚本”还是“核心函数”。
```

------

### 精简版提示词（更适合先让 Codex 开始扫仓库）

```text
请对当前 MATLAB 科研仓库做一次“结构化重构”，目标是统一 benchmark 入口、拆分路径初始化/配置标准化/模式解析/导出逻辑，并保留原始算法逻辑与原始 CEC 测试逻辑不变。

要求：
1. 不改 baseline、improved 算法的核心优化行为；
2. 不改 run_all_compare 的核心 benchmark 语义；
3. 把多处 run_*.m 收口为统一主入口；
4. 把旧入口保留为兼容壳；
5. 把 protocol_snapshot、summary markdown、aggregate export 抽离成独立函数；
6. 把 smoke/formal/ablation 尽量改为配置驱动；
7. 输出“重构计划 + 文件映射表 + 实际修改 + 验证结果”；
8. 优先做低风险兼容式重构，不要一次性删旧逻辑。

先分析仓库调用链与入口关系，再开始改。
```

------

### 适合你这次场景的 Codex 会话设置建议

这个是按你之前希望我默认考虑的 **Skills / 聊天指令 / Hooks** 一起给的。

#### 聊天指令（Chat Instructions）

把下面这段放到 Codex 会话开头：

```text
本次任务是“科研代码结构化重构”，优先级顺序为：
1. 保持原始算法逻辑与 CEC 测试逻辑不变；
2. 提高执行链清晰度与可维护性；
3. 保持旧入口兼容；
4. 输出可复核的重构说明与验证说明。

回答时请始终区分：
- 已确认事实
- 基于代码结构的推断
- 修改建议
- 已实施改动
- 尚未验证部分

不要只给概括，请给文件级别变更说明。
```

#### Hooks（钩子）

让 Codex 每次改完自动做这几类检查：

```text
改动后自动执行：
1. 扫描 run_*.m 入口是否仍存在断链；
2. 检查新增函数是否被引用；
3. 检查旧入口是否仍可调用新核心；
4. 检查 protocol_snapshot 导出字段是否保持兼容；
5. 检查是否误改算法文件核心更新逻辑；
6. 输出一份“本轮改动风险点”。
```

#### Skills（技能）

若 Codex 环境支持自定义技能，建议这次启用/模拟以下技能职责：

- `repo-structure-auditor`：扫描入口、调用链、重复职责
- `matlab-refactor-guardian`：防止误改算法行为
- `benchmark-compat-checker`：检查 benchmark 输入输出兼容性
- `research-export-validator`：检查 protocol / summary / report 导出兼容性

------

### 我建议你发给 Codex 的顺序

#### 第一条

发我上面那段 **主提示词**

#### 第二条

再补这句：

```text
这次先不要急着全量大改。先完成：
1. 重构计划
2. 文件映射表
3. 第一批低风险改动（路径初始化、配置标准化、协议快照抽离、run_experiment_unified 兼容壳化）
做完后再继续第二批。
```

这样更稳，不容易一口气改太猛。

------

### 论文撰写或修改建议

这次重构如果后面要写进论文、周报或中期检查，不建议写成“整理代码”。
更适合写成：

> 对实验管理框架进行了结构化整理，在不改变核心优化算法与基准测试逻辑的前提下，将实验入口、配置解析、模式控制、结果导出与协议记录解耦，提升了实验流程的可维护性、可复现性与后续扩展效率。

这个表述更像真实科研工程工作，而不是单纯“改文件夹”。

------

### 这次你能学到什么 / 对以后有什么帮助 🌱

这次最关键的收获，其实是学会一种很重要的科研代码升级方式：

> **不是推倒重来，而是在“逻辑冻结”的前提下做结构回收。**

可以把它理解成盖楼 🏗️：

- 前期先把楼盖起来最重要
- 但楼层越加越多后，电梯、楼梯、管线要重新整理
- 整理的是“基础设施”，不是把住户全赶走重建

你这次让 Codex 做的，本质上就是这件事。

------

### 当前总体任务进度

- **当前阶段**：准备进入代码结构回收与统一入口重构
- **已完成**：重构目标、边界条件、Codex 执行提示词、兼容约束、验证要求全部明确
- **下一步**：把上面的主提示词发给 Codex，让它先输出“重构计划 + 文件映射表 + 第一批低风险改动”